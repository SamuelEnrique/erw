# Energy Research Warehouse (ERW) Python requirements.
#
# Python 3.10 to 3.13:   pip install -r requirements.txt
# Python 3.14:           pip install --no-deps -r requirements-py314.txt
#   gridstatus 0.36.0 pins lxml~=5.3, which has no Python 3.14 wheel and does
#   not build from source on Windows. requirements-py314.txt is the exact,
#   tested environment with lxml 6.1.3 instead. See SESSION_1_REPORT.md.
gridstatus==0.36.0
pandas>=2.2,<3
