Session 5 of the Energy Research Warehouse (ERW). Read CLAUDE.md, ARCHITECTURE.md, docs/datastandard.md, docs/coverage.md, .claude/skills/erw-add-connector/SKILL.md, SESSION_3_REPORT.md and SESSION_4_REPORT.md first. Same non-negotiables: real data only, fail loudly, no em dashes, never delete existing files, commit after each task, do not push, do not stop to ask questions. API keys are in .env at the repo root (EIA_API_KEY, ANTHROPIC_API_KEY, PJM_API_KEY); load them with python-dotenv, add .env to .gitignore before anything else, and never print or commit a key. In the GitHub workflow, read them from secrets of the same names.

Human rulings on the open questions from sessions 2 to 4: open a GitHub issue when a market has failed three daily runs in a row; no retention window, history moves to Redivis later; append each run's per-ISO status to a tracked warehouse/metadata/run_status.csv; add a durable source registry warehouse/metadata/sources.csv merged like the tables so cite() never loses a report; keep CAISO on RTD 15-minute means for now; for ISO-NE add its hourly final real-time LMPs as a second table isone_rtm_zone_prices_hourly.csv (variable lmp_rtm) and keep the 5-minute-derived table when complete; keep erw installable from the repo only until Redivis exists; give fetch optional start, end and node arguments that subset rows; keep raw files local and prune raw runs older than 14 days, keeping every manifest.csv; the geo footprint lists and the SPP footprint will be checked by the human separately. PJM data is licensed for internal use only; add a license column to warehouse/metadata/coverage.csv and to docs/datastandard.md, mark PJM tables internal and every other table public, and make erw.coverage() expose it so the public site can filter on it. Throttle the PJM connector to at most 5 requests per minute. If PJM_API_KEY is empty, skip Task 1 entirely and say so in the report. Apply every ruling in this session and record each in docs/datastandard.md or the relevant file.

TASK 1. PJM.
Add pull_pjm to iso_prices.py using gridstatus with the PJM key: day-ahead and real-time hourly LMPs for the 30 last operating days (America/New_York) at PJM's trading hubs (WESTERN HUB, EASTERN HUB, DOMINION HUB, AEP GEN HUB, CHICAGO HUB, N ILLINOIS HUB, NEW JERSEY HUB, OHIO HUB, ATSI GEN HUB, PENELEC HUB). PJM real-time is 5-minute; aggregate to 15-minute means like the others, and also write the hourly integrated real-time table PJM publishes (variable lmp_rtm) as pjm_rtm_hub_prices_hourly.csv. Follow the skill file exactly. Add PJM to run_daily.sh and the workflow.

TASK 2. Hourly demand and generation, all balancing authorities.
New connector warehouse/connectors/eia930.py using the EIA API v2 (EIA_API_KEY), route electricity/rto: for each of the seven ISOs plus the US total, pull the last 30 days of hourly demand (variable demand_mw), day-ahead demand forecast (demand_forecast_mw), net generation (net_generation_mw) and net generation by fuel type (one variable per fuel, net_generation_<fuel>_mw). Series shape, entity eia930:<BA code>, unit MW, freq PT1H, source the exact API route and parameters. Write one table per ISO per family: eia930_<ba>_demand.csv and eia930_<ba>_generation.csv. Same completeness rule. Add to run_daily.sh and the workflow.

TASK 3. Fuels.
New connector warehouse/connectors/eia_fuels.py using the EIA API v2: Henry Hub natural gas spot price (daily, USD/MMBtu), WTI Cushing spot price (daily, USD/bbl) and Brent spot (daily, USD/bbl), full history the API offers, entities eia:henry_hub, eia:wti_cushing, eia:brent, freq P1D. Table eia_fuel_spot_prices.csv. Extend the validator's unit and freq rules if these expose a gap in the standard, and record the change. Add to run_daily.sh and the workflow.

TASK 4. Rulings and package.
Apply every human ruling above: run_status.csv, sources.csv and cite() reading from it, the three-failures issue rule, ISO-NE hourly table, raw pruning, fetch subsetting, the license column. Update the package tests for the new tables and arguments; every test must pass. Rerun the full daily sequence locally once and confirm every table validates and the coverage table lists every new file.

TASK 5. Report.
SESSION_5_REPORT.md in the usual format, plus a paragraph listing which of the 20 platform tools now have their data layer in place. Final commit.